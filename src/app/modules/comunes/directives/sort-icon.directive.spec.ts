import { SortIconDirective } from './sort-icon.directive';

describe('SortIconDirective', () => {
  it('should create an instance', () => {
    const directive = new SortIconDirective();
    expect(directive).toBeTruthy();
  });
});
